# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Configuration (IMPORTANT: Change JWT_SECRET in production to a secure random string!)
JWT_SECRET=change-me-to-a-secure-random-string-with-at-least-32-characters
JWT_EXPIRES_IN=7d

# CORS Configuration (Add your production frontend URL for production deployment)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Production example:
# NODE_ENV=production
# JWT_SECRET=your-super-secure-random-jwt-secret-key-here-with-64-characters
# CORS_ALLOWED_ORIGINS=https://your-domain.com
# FRONTEND_URL=https://your-domain.com
